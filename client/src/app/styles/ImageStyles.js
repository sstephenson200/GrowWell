import { StyleSheet } from "react-native";

const ImageStyles = StyleSheet.create({
    icon: {
        width: 30,
        height: 30
    },
    iconLabel: {
        fontSize: 20,
        paddingLeft: 7
    }
});

export default ImageStyles;